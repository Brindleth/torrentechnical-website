HERO VIDEO
==========

Put your exported Flow / Veo flight video in THIS folder, named exactly:

    hero.mp4

The homepage hero plays it full-screen (muted, looping) behind the headline.

Optional: add a still frame named  hero-poster.jpg  (shown before the video loads).

Until hero.mp4 is here, the hero shows a solid navy background — no error.
